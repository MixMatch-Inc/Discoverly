{
  "name": "discoverly",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "mobile",
    "web"
  ],
  "scripts": {
    "bootstrap": "npm install --workspaces",
    "lint": "npm run lint -w backend && npm run lint -w mobile && npm run lint -w web",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w mobile && npm run typecheck -w web",
    "test": "npm run test -w backend"
  }
}
